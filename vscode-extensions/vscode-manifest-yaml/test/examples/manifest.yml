applications:
- name: foo
  foo: bar
  builpack: java
  buildpack: java_buildpack
  memory: 1G
  command: java main.java
  disk_quota: 1024M
  domain: pivotal.io
  domains:
  - pivotal.io
  - other.org
  env:
    RAILS_ENV: production
    RACK_ENV: production

